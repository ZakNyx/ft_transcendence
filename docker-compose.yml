version: '3.8'
services:
  post:
    image: postgres:13
    ports:
      - "5434:5434"
    env_file:
    - ./backend/.env
    restart: always
  backend:
    build: 
      context: ./backend
    ports:
      - 3000:3000
    depends_on:
      - post
    restart: always
  frontend:
    build: 
      context: ./frontend
    ports:
      - 5174:5174
    depends_on:
      - backend
    restart: always
networks:
  default:
